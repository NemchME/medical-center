services:
  postgres:
    image: postgres:16
    container_name: medicina-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: medicina
    ports:
      - "54335:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: